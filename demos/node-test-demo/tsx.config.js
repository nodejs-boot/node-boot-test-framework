export default {
  // Enable decorator support
  compilerOptions: {
    experimentalDecorators: true,
    emitDecoratorMetadata: true,
    target: 'ES2022',
    module: 'ESNext',
    moduleResolution: 'node'
  }
}

